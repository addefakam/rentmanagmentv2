// ============================================================================
// page.tsx — Phase 4 platform console (client shell over /api/platform).
// Seven sprint increments (S1-S7), Phase 3 evidence and the Gate G4 package,
// all trilingual per CR-01 (Amharic / English / Afan Oromo).
// ============================================================================

import PlatformConsole from "@/components/platform/console";

export const dynamic = "force-dynamic";

export default function RentControlPlatform() {
  return <PlatformConsole />;
}
